import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('EduTrack app smoke test', (WidgetTester tester) async {
    // Minimal smoke test — full widget testing requires mocked Supabase
    expect(true, isTrue);
  });
}
